---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Header from "@/components/layout/Header.astro";
import Footer from "@/components/layout/Footer.astro";
import HeroSection from "@/components/sections/HeroSection.astro";
import AboutSection from "@/components/sections/AboutSection.astro";
import ApartmentsSection from "@/components/sections/ApartmentsSection.astro";
import ProcessSection from "@/components/sections/ProcessSection.astro";
import TestimonialsSection from "@/components/sections/TestimonialsSection.astro";
import ContactSection from "@/components/sections/ContactSection.astro";
import { LOCALES } from "@/data/locales";
import { HERO_IMAGE } from "@/data/apartments";
import { SOCIALS } from "@/data/site";
import {
  buildFaqSchema,
  buildLodgingSchema,
  buildOrganizationSchema,
  buildWebsiteSchema
} from "@/lib/structured-data";

const locale = LOCALES.hr;

const alternates = [
  { hreflang: "hr", path: "/" },
  { hreflang: "en", path: "/en/" },
  { hreflang: "x-default", path: "/" }
] as const;

const structuredData = [
  buildWebsiteSchema("/"),
  buildOrganizationSchema(),
  buildLodgingSchema("hr"),
  buildFaqSchema(locale.landing.faq)
];
---

<BaseLayout
  locale="hr"
  skipToContentLabel={locale.skipToContent}
  meta={{
    title: locale.seo.home.title,
    description: locale.seo.home.description,
    canonicalPath: "/",
    locale: "hr"
  }}
  alternates={alternates}
  structuredData={structuredData}
  lcpImagePath={HERO_IMAGE.src}
>
  <Fragment slot="header">
    <Header
      nav={locale.nav}
      brand="Apartmani Dada"
      active="home"
      switchHref="/en/"
      switchLabel="EN"
      links={{
        home: "/",
        apartments: "/#apartments-title",
        about: "/#about-title",
        book: "/book-now",
        contact: "/#contact-title"
      }}
    />
  </Fragment>

  <HeroSection
    kicker={locale.hero.kicker}
    headline={locale.hero.headline}
    subheadline={locale.hero.subheadline}
    bullets={locale.hero.bullets}
    ctaText={locale.hero.cta}
    ctaHref="/#apartments-title"
    image={{
      src: HERO_IMAGE.src,
      alt: HERO_IMAGE.alt.hr,
      width: HERO_IMAGE.width,
      height: HERO_IMAGE.height
    }}
  />
  <AboutSection title={locale.about.title} subtitle={locale.about.subtitle} cards={locale.about.cards} />
  <ApartmentsSection locale="hr" title={locale.apartments.title} subtitle={locale.apartments.subtitle} />
  <ProcessSection title={locale.process.title} subtitle={locale.process.subtitle} steps={locale.process.steps} />
  <TestimonialsSection locale="hr" title={locale.testimonials.title} subtitle={locale.testimonials.subtitle} />
  <ContactSection locale="hr" copy={locale.contact} />

  <Fragment slot="footer">
    <Footer
      brand="Apartmani Dada"
      text={locale.footer.text}
      copyright={locale.footer.copyright}
      facebookUrl={SOCIALS.facebook}
    />
  </Fragment>
</BaseLayout>
